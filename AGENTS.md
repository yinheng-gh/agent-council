# Agent Council

## 核心原则（每次任务开始前和完成后必须检查）

1. 阅读 [经验教训](./LESSONS.md)，确保不再犯已记录的错误
2. 完成任务后，重新审视用户的原始问题和需求，检查自己的实现是否与用户要求的方向有冲突
3. 严格遵守各 AGENTS.md 中的具体配置和约束，执行命令时从文档中复制而非凭记忆输入
4. 当犯了不该犯的错误时，必须更新 [经验教训](./LESSONS.md)，记录错误内容并增加犯错次数
5. 始终使用中文回答问题，可以使用英文进行思考

## 通用开发规则

### 先调研再实现

- 对不熟悉的系统能力，**必须先查阅官方文档或最新的技术文章**，确认推荐的 API 和模式后再编码
- 当连续做了 2 次以上 workaround 仍无法解决问题时，应停下来重新调研，很可能是根本方向错了

### AGENTS.md 维护原则

- AGENTS.md 只记录**开发原则、约束、命令、已知坑点**等 agent 无法从代码中自行获取的信息
- **不要**在 AGENTS.md 中写项目结构树、API 端点详细文档、数据模型表等可通过读代码获知的内容
- API 文档的权威来源是 `api.http`，数据模型看 `src/db/schema.ts`，项目结构 agent 自己 ls

### Git使用原则

- 保持原子提交，如果我明确让你**无人值守的一次性**开发多个功能，或者一个修改中应该包含多个原子内容，那就自行先进行原子提交，哪怕这次提交可能有问题；如果我没有让你无人值守的开发，请**不要主动提交**，直到我说commit #<issue-number>

#### git commit message格式

遵守Conventional Commits格式，具体格式如下：

```
<type>(<scope>): <subject> #<issue-number>

<body>

<footer>
```

解释：

- scope为server, app, ray, web等，如果一个改动涉及多个scope，那就不需要写了
- issue-number要么我一开始在提要求的时候说过（一般会简称#number这样，如#1），如果没说，则默认提交到上一个commit的issue number上
- footer中包含你的基本信息，让人知道是谁来开发的
